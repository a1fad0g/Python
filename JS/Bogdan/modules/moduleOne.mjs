const myName = 'Ogpus'
const sum = (a, b) => a + b
export default sum
console.log(myName)

const one = 1
const two = 'wtho'
export {
    one,
    two
}

const mult = (a, b) => a * b
export {
    sum,
    mult
}
